<form class="{{cssClass}}" autocomplete="off">
    <header class="sheet-header">
        <img src="{{item.img}}" data-edit="img" title="{{item.name}}" height="64" width="64"/>
        <h1><input name="name" type="text" value="{{item.name}}" placeholder="{{ localize 'Name' }}"/></h1>
    </header>
    
    <div class="editor-section-container" style="min-height: 150px;">
        <div class="sheet-editor-container">
            <h2>{{localize "godbound.common.description"}}</h2>
            {{editor content=data.data.description target="data.description" button=true owner=owner editable=editable}}
        </div>
        <div class="sheet-editor-container">
            <h2>{{localize "godbound.common.notes"}}</h2>
            {{editor content=data.data.notes target="data.notes" button=true owner=owner editable=editable}}
        </div>
    </div>
    <table class="item-table">
                <tr>
                    <th>
                        {{localize "godbound.lowMagic.masteryLevel"}}
                    </th>
                    <th>
                        {{localize "godbound.lowMagic.castingType"}}
                    </th>
                </tr>
                <tr>
                    <td class="item-table-data">
                        <select name="data.masteryLevel" class="item-table-select">
                            {{#select data.data.masteryLevel}}
                            {{#each config.lowMagic.lowMagicMasteries as |name type|}}
                            <option value="{{type}}">{{localize name}}</option>
                            {{/each}}
                            {{/select}}
                        </select> 
                    </td>
                    <td class="item-table-data">
                        <select name="data.castingType" class="item-table-select">
                            {{#select data.data.castingType}}
                            {{#each config.lowMagic.castingTypes as |name type|}}
                            <option value="{{type}}">{{localize name}}</option>
                            {{/each}}
                            {{/select}}
                        </select> 
                    </td>
                </tr>
            </table>
    
</form>
